from aiogram import Router

# TODO: Add allowed channels to config..
# from filters import filter_access_channels

channels_router = Router(name='channels_router')

# channels_router.message.filter(filter_access_channels)
# channels_router.callback_query.filter(filter_access_channels)
